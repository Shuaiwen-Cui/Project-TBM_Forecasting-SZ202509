# 论文图表与结论（由 03-Analyses 生成）

## 图表引用

- **图 1**：数据集参数相关性（前 12 维掘进参数相关系数热力图）。
- **图 2**：预测精度（R²、MSE、MAE、RMSE）在 20 组配置下的表现。
- **图 3**：计算性能（训练时间、推理时间、内存）在 20 组配置下的表现。
- **图 4**：R² 随序列长度与预测长度的三维曲面。
- **图 5**：R² 热力图（行=预测长度，列=序列长度）。
- **图 6**：精度-效率权衡（R² vs 训练时间，点标注 seq_len-pred_len）。
- **图 7**：贯入度 — 多配置（不同 seq_len×pred_len）真实值与 Transformer 预测值曲线。
- **图 8**：推进油缸总推力 — 多配置真实值与 Transformer 预测值曲线。
- **图 9**：刀盘扭矩 — 多配置真实值与 Transformer 预测值曲线。

图存放于 `figures/`，表存放于 `tables/`（`table_metrics.csv`、`table_metrics.tex`、`table_best_worst.txt`）。  
运行 `run_analysis.py` 可一键生成全部图表并写入 `分析摘要.txt`。图表采用中文宋体、英文与数字 Times New Roman，符合学术规范。

（此处可后续粘贴论文中“实验结果”“讨论”等段落的草稿。）
